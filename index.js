/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {WalletFlatList} from './src/screens/WalletFlatList';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => WalletFlatList);
